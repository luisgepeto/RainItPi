<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModal">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="myModalLabel"><strong>Confirm Action</strong></h4>
            </div>
            <div class="modal-body">
                <div class="row">
                    <div id="message" class="col-md-8"></div>
                    <div class="col-md-4" id="modalContent"></div>
                </div>
                <div class="row">
                    <div class="col-md-12">
                        &nbsp;
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-12" id="longModalContent"></div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-danger" id="button"></button>
            </div>
        </div>
    </div>
</div>
<script type="text/javascript">
    (function() {
        this.ModalDeleteProperties = this.ModalDeleteProperties || {};
        ModalDeleteProperties.Show = function (onShowAction) {
            $('#myModal').modal('show');
            if (onShowAction) {
                onShowAction();
            }
        }
       
        var button = (function() {
            var properties = {};
            function addClickListener(functionToExecute, functionParameter) {
                $('#button').off();
                $('#button').click(function () {
                    functionToExecute(functionParameter);
                });
            }
            properties.Initialize = function (parameters, functionToExecute, functionParameter) {
                $('#button').html(parameters.ButtonName);
                $('#message').html(parameters.Message);
                $('#modalContent').html("");
                $('#longModalContent').html("");
                if (parameters.ModalContent) {
                    parameters.ModalContent.appendTo($('#modalContent'));
                }
                if (parameters.LongModalContent) {
                    parameters.LongModalContent.appendTo($('#longModalContent'));
                }
                addClickListener(functionToExecute, functionParameter);
            }
            return properties;
        }());

        ModalDeleteProperties.Initialize = function (parameters, functionToExecute, functionParameter) {
            button.Initialize(parameters, functionToExecute, functionParameter);
        }
    }());
</script>